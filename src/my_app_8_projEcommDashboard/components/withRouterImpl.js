// import { useNavigate } from "react-router";

// TO MATCH withRouter IN react-router-dom version 5.
// export const withRouter = (Component) => {
//   const Wrapper = (props) => {
//     const history = useNavigate();
//     return <Component history={history} {...props} />;
//   };
//   return Wrapper;
// };
